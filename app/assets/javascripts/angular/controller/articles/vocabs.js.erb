app.controller('vocabCtrl', ['$scope', '$http', '$location', function($scope, $http, $location){

  var url = "<%= ENV['URL'] %>" || "http://localhost:3000";

  $http.get(url + '/articles').success(function(response){
    console.log(response);
    $scope.articles = response;
  });
}]);










//idioms and flash cards